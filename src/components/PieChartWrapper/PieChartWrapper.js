import React, { Component } from 'react';
// import PieChart from '../PieChart/PieChart';

// import styles from './PieChartWrapper.module.css'

export default class PieChartWrapper extends Component {
    // //Creates a new instance of the PieChart class and attaches to chart ref
    // //Renders container for information from mouseover event on PieChart
    // componentDidMount() {
    //     this.setState({ chart: new PieChart(this.refs.chart, this.refs.info) })
    // }

    render() {
        return (
            // <div className={styles.chartContainer}>
            //     <div ref="chart"></div>
            //     <div ref="info" className={styles.info}></div>
            // </div>
            <div>Pie Chart</div>
        );
    }
}
